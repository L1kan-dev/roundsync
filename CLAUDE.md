# RoundSync

Before doing anything else in this session, read **`AI_CONTEXT.md`** (repo
root) in full and follow its reading list. It's the entry point for every
new session working on this project — it exists specifically so this
doesn't have to be requested manually each time.
